# Local Plugin Specs

Add LazyVim plugin additions and overrides here as `*.lua` files.

Keep `lua/config/lazy.lua` for bootstrap and high-level extras. Use this folder
for local choices such as plugin options, new keymaps tied to a plugin, or
disabled defaults.
